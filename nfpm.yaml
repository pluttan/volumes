# nfpm configuration for multi-platform package generation
name: vol
arch: "${GOARCH}"
platform: linux
version: "${VERSION}"
maintainer: "pluttan <pluttan@example.com>"
description: "Universal build tool with beautiful terminal output"
vendor: "pluttan"
homepage: "https://github.com/pluttan/volumes"
license: "MIT"

contents:
  - src: ./dist/vol
    dst: /usr/local/bin/vol
    file_info:
      mode: 0755
  - src: ./completions/bash/vol
    dst: /usr/share/bash-completion/completions/vol
    file_info:
      mode: 0644
  - src: ./completions/zsh/_vol
    dst: /usr/share/zsh/site-functions/_vol
    file_info:
      mode: 0644

# Overrides for specific formats
overrides:
  deb:
    depends:
      - libc6
  rpm:
    depends:
      - glibc
